// joins paths based on best fitness
module.exports = function (paths) {
    
    const genes = new Set();
    


    const globalPath = [];

    paths[0].forEach(cell => {
        globalPath.push(cell)
    });


}